spring: # Spring에서는 resource 정보가 없으면 기본적으로 Memory모드로 동작시킴
#  datasource:
#    url: jdbc:h2:mem:test # h2를 메모리 모드로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
